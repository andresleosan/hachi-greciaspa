# Firestore Schema

Documento de referencia de las colecciones de Firestore del proyecto `hachi-greciaspa`. Mantenido en sincronía con el código; cualquier cambio de schema debe reflejarse acá y en `firestore.rules`.

Última actualización: 2026-08-07 (Fase 3, mascotas, email transaccional y hardening de reservas).

## Convenciones

- Los timestamps de flujos que escriben directamente desde el cliente (`users.createdAt`, `mascotas.createdAt`/`updatedAt` y `mensajes.createdAt`) se solicitan con `serverTimestamp()` desde el SDK cliente; el valor final lo asigna el servidor de Firestore.
- Los timestamps de `reservas`, `bookingGuards`, `bookingSlotGuards`, `recordatorios` y `confirmaciones` los genera Functions/Admin SDK server-side. No deben documentarse como timestamps generados por el cliente aunque el flujo se inicie desde la UI.
- Los "snapshots" (`serviceName`, `userName`, `price` en `reservas`) se guardan por inmutabilidad histórica: si el servicio cambia de nombre o precio después de creada la reserva, la reserva conserva los valores del momento de creación.

## Colecciones

### `users/{uid}`

Perfil de usuario. Creado en `register()` (`src/services/auth.ts:29`).

| Campo | Tipo | Generated by | Notas |
|---|---|---|---|
| `email` | string | client | bloqueado a mutación por usuario (rule N1) |
| `displayName` | string \| null | client | |
| `role` | 'client' \| 'admin' | client (always 'client' on signup) | admin reasignado por `set-admin.js` |
| `createdAt` | Timestamp | client | `serverTimestamp()` |

**Reglas:** create si auth.uid == uid && role=='client'; read/update propio o admin; update no puede tocar `role` ni `email` para no-admin (regla N1 de `AUDITORIA.md`).

**Custom claims (Auth):** opcional `admin: true` en el token, complementario al role en Firestore. `set-admin.js` lo setea. Las rules de `isAdmin()` loen ambos.

---

### `servicios/{docId}`

Catálogo de servicios del spa. Leído públicamente por `Servicios.tsx`.

| Campo | Tipo | Notas |
|---|---|---|
| `name` | string | |
| `description` | string | |
| `durationMin` | number | |
| `imageUrl` | string \| null | path estático o URL |
| `category` | string | (masajes, faciales, etc.) |
| `order` | number | orden de visualización |
| `active` | boolean | permite soft-hide en UI |

**Reglas:** solo lectura pública; escritura admin-only.

---

### `precios/{docId}`

Tarifario. Schema `PriceItem` definido en `src/types/precios.ts`.

| Campo | Tipo | Notas |
|---|---|---|
| `name` | string | obligatorio, 1–120 caracteres |
| `price` | number \| null | MXN, mayor o igual a cero |
| `priceHigh` | number \| null | MXN, mayor o igual a `price` cuando ambos existen |
| `unit` | string \| null | máximo 80 caracteres |
| `note` | string \| null | máximo 240 caracteres |
| `category` | string \| null | máximo 80 caracteres |

**Reglas:** lectura pública; creación y actualización admin-only con allowlist de campos y validación de tipos/rangos; eliminación admin-only (C1 de AUDITORIA.md).

---

### `reservas/{resId}`

Citas reservadas. Es la colección central de Fase 2.

| Campo | Tipo | Generated by | Notas |
|---|---|---|---|
| `userId` | string | Functions (auth.uid) | dueño de la reserva |
| `userName` | string \| null | Functions | snapshot del perfil/token |
| `userEmail` | string \| null | Functions | snapshot del token/perfil |
| `serviceId` | string | Functions (input callable) | FK a `servicios/{docId}` |
| `mascotaId` | string \| null | Functions (input callable) | FK opcional a `mascotas/{mascotaId}`; debe pertenecer al mismo usuario |
| `serviceName` | string | Functions | snapshot canónico del nombre |
| `price` | number \| null | Functions | `null` en creación cliente; no lo controla el payload |
| `date` | string | Functions (input callable) | ISO "YYYY-MM-DD", futura en `America/Mexico_City` |
| `timeSlot` | string | Functions (input callable) | "HH:mm" 24h |
| `durationMin` | number | Functions | snapshot canónico desde el catálogo |
| `notes` | string \| null | Functions (input callable) | comentarios del cliente |
| `empleadoId` | string \| null | Functions/Admin SDK | empleado asignado; los documentos legacy pueden omitirlo |
| `status` | 'pending' \| 'confirmed' \| 'cancelled' \| 'completed' | Functions/admin | creación cliente siempre `pending` |
| `createdAt` | Timestamp | Functions/admin | server-side |
| `createdBy` | 'client' \| 'admin' | Functions/admin | qué flujo originó la reserva |

**Flujo cliente:** el wizard conserva su frontera `createReserva()`, pero envía únicamente `serviceId`, `date`, `timeSlot`, `mascotaId` y `notes` a la callable `createReserva`. Functions valida el contrato y crea `reservas`; el cliente no usa `addDoc` ni decide identidad, snapshots, precio, duración, estado o `createdBy`.

**Índices:** compuestos `userId ASC + createdAt DESC` ("mis reservas"), `userId ASC + mascotaId ASC` (historial por mascota), `userId ASC + date ASC` (métricas diarias por cliente) y `serviceId ASC + date ASC + timeSlot ASC` (consultas existentes) están en `firestore.indexes.json`. Para la callable se agregaron de forma aditiva `userId ASC + status ASC` (conteo de reservas activas por usuario) y `serviceId ASC + date ASC + status ASC` (disponibilidad activa por servicio y fecha).

**Rollback de índices:** los índices `userId ASC + status ASC` y `serviceId ASC + date ASC + status ASC` son aditivos y no modifican documentos. Para revertirlos, retirar únicamente esos dos bloques de `firestore.indexes.json` después de retirar el código que los consulta y aplicar la configuración autorizada; no borrar reservas ni ejecutar una migración destructiva. No existe backfill ni migración de datos para esta decisión.

**Reglas:** ver `firestore.rules:55-82`, ADR-002 y ADR-008. El cliente no puede crear directamente una reserva: `allow create: if false`; Functions/Admin SDK bypassa Rules para crear el documento canónico. El propietario solo puede cancelar con el cambio exacto `status -> 'cancelled'` cuando la reserva está `pending` o `confirmed`; las escrituras directas del cliente sobre `date`/`timeSlot` son denegadas y no puede cambiar campos adicionales. `rescheduleMyReserva` llama exclusivamente a la callable `rescheduleReserva`, que usa Admin SDK y es la autoridad para validar fecha/hora futura y disponibilidad del slot en una transacción server-side. La callable de creación aplica cuota de 3 intentos por 15 minutos, límite de 10 activas, fecha futura, servicio activo, ownership de mascota y solapamiento transaccional. Admin conserva la edición completa y la asignación automática también usa Admin SDK.

Los documentos existentes sin `empleadoId` son compatibles y se interpretan como `null`. El backfill opcional de abajo solo normaliza esa ausencia; nunca elige ni asigna un empleado.

---

### `bookingGuards/{uid}`

Estado privado de cuota de creación de reservas. Solo la callable `createReserva`
mediante Admin SDK lo lee y actualiza dentro de la transacción.

| Campo | Tipo | Generated by | Notas |
|---|---|---|---|
| `uid` | string | Functions | coincide con el documento y el `auth.uid` |
| `windowStartedAt` | Timestamp | Functions | inicio de la ventana de 15 minutos |
| `attempts` | number | Functions | intentos consumidos; máximo efectivo de 3 por ventana |
| `updatedAt` | Timestamp | Functions | última actualización server-side |

**Ownership and visibility:** `allow read, write: if false` para clientes. No
es una colección de perfil ni un dato editable por el usuario; su persistencia
permite serializar invocaciones concurrentes y conservar evidencia operativa.

**Rollback:** colección aditiva y sin backfill. Si se deshabilita la callable,
se conservan los guards para auditoría; no se borran documentos como parte del
rollback.

---

### `bookingSlotGuards/{encodeURIComponent(serviceId)}__{date}`

Lock operativo determinista para serializar la disponibilidad entre usuarios
del mismo servicio y día. Functions/Admin SDK lo crea lazy, al crear o
reagendar una reserva; no existe backfill ni migración de reservas
existentes.

| Campo | Tipo | Generated by | Notas |
|---|---|---|---|
| `serviceId` | string | Functions | coincide con el servicio consultado |
| `date` | string | Functions | ISO `YYYY-MM-DD` del día bloqueado |
| `updatedAt` | Timestamp | Functions | última actualización server-side |

**Ownership and visibility:** solo Firebase Functions mediante Admin SDK lee y
actualiza el documento dentro de la transacción de `createReserva` o
`rescheduleReserva`. El acceso
de cliente está denegado con `allow read, write: if false`; Admin SDK bypassa
Rules. Todas las solicitudes del mismo servicio/día comparten el documento,
por lo que la contención queda acotada a esa combinación y no al catálogo
completo. La consulta posterior sigue devolviendo todos los activos del
servicio/día y no tiene un límite de documentos conocido actualmente.

**Rollback:** colección operativa aditiva, lazy y sin backfill. Si se
deshabilita la callable, se conservan los locks para auditoría; el rollback no
los borra ni ejecuta una migración destructiva. El lock evita carreras, pero no
sustituye una futura estrategia de particionamiento o retención de datos.

---

### `recordatorios/{id}`

Registro de control de recordatorios de cita. El ID es determinístico, con el formato `reminder-${encodeURIComponent(reservaId)}`, para que las ejecuciones horarias reutilicen el mismo documento y no envíen recordatorios duplicados.

| Campo | Tipo | Generated by | Notas |
|---|---|---|---|
| `reservaId` | string | Functions | FK a `reservas/{resId}` |
| `status` | 'pending' \| 'sent' \| 'failed' | Functions | estado del envío |
| `attempts` | number | Functions | intentos acumulados; máximo operativo de tres |
| `scheduledFor` | Timestamp | Functions | fecha y hora objetivo del recordatorio |
| `sentAt` | Timestamp \| null | Functions | instante de envío exitoso |
| `lastAttemptAt` | Timestamp \| null | Functions | último intento de envío |
| `lastError` | string \| null | Functions | categoría de error sanitizada |
| `processingLockUntil` | Timestamp \| null | Functions | vencimiento del lock transaccional temporal |
| `processingToken` | string \| null | Functions | token único que permite aplicar solo la transición del worker dueño del lock |
| `nextAttemptAt` | Timestamp \| null | Functions | no se intenta antes de este instante tras una falla retryable |
| `providerMessageId` | string \| null | Functions | ID devuelto por Resend en el envío exitoso |
| `createdAt` | Timestamp | Functions | creación del registro |
| `updatedAt` | Timestamp | Functions | última transición del registro |

**Lifecycle:** Functions crea el documento en `pending` con `attempts: 0`, `processingToken: null`, `nextAttemptAt: null` y `providerMessageId: null`, adquiere un lock temporal antes de cada envío y aumenta `attempts`. Cada adquisición asigna un token único; las transiciones finales solo se aplican si conservan ese token. El ID determinístico `reminder-${encodeURIComponent(reservaId)}` también se envía como la clave externa de idempotencia del proveedor. En éxito cambia a `sent`, establece `sentAt` y conserva el `providerMessageId`; en una falla retryable cambia a `failed`, conserva una categoría sanitizada en `lastError`, establece `nextAttemptAt` con backoff acotado y libera el lock. En una falla permanente o éxito se limpia `nextAttemptAt`. Las reservas no se modifican por un fallo de email. Los reintentos posteriores solo proceden mientras `attempts < 3` y después de `nextAttemptAt`.

**Ownership and visibility:** solo Firebase Functions mediante Admin SDK crea o actualiza estos documentos; el Admin SDK no depende de los permisos de cliente. Un administrador puede leerlos para operación y debugging. Guest y client no tienen lectura ni escritura; las reglas mantienen `allow write: if false` porque las escrituras de Functions bypassan las reglas de cliente.

**Rollback:** el cambio es aditivo. Para revertirlo, deshabilitar y retirar el scheduled function, conservar `recordatorios` para auditoría y, si corresponde, retirar por separado el índice agregado. No borrar documentos ni ejecutar una migración destructiva.

---

### `confirmaciones/{id}`

Registro de control del email inmediato de confirmación. El ID es determinístico, con el formato `confirmation-${encodeURIComponent(reservaId)}`, para que los reintentos del trigger reutilicen el mismo documento y no reenvíen una confirmación ya entregada.

| Campo | Tipo | Generated by | Notas |
|---|---|---|---|
| `reservaId` | string | Functions | FK a `reservas/{resId}` |
| `status` | 'pending' \| 'sent' \| 'failed' | Functions | estado del envío |
| `attempts` | number | Functions | intentos acumulados; máximo operativo de tres |
| `sentAt` | Timestamp \| null | Functions | instante de envío exitoso |
| `lastAttemptAt` | Timestamp \| null | Functions | último intento de envío |
| `lastError` | string \| null | Functions | categoría de error sanitizada |
| `processingLockUntil` | Timestamp \| null | Functions | vencimiento del lock temporal |
| `processingToken` | string \| null | Functions | token del worker propietario del lock |
| `nextAttemptAt` | Timestamp \| null | Functions | backoff tras un fallo retryable |
| `providerMessageId` | string \| null | Functions | ID devuelto por Resend |
| `createdAt` | Timestamp | Functions | creación del registro |
| `updatedAt` | Timestamp | Functions | última transición |

**Lifecycle:** `onReservaConfirmationCreated` escucha únicamente la creación de `reservas/{resId}`. Crea el documento en `pending`, adquiere un lock transaccional y envía el resumen con la clave externa `confirmation-${encodeURIComponent(reservaId)}`. En éxito cambia a `sent`; ante 429, timeout o 5xx registra `failed`, conserva un error sanitizado y usa backoff hasta tres intentos; ante datos inválidos o fallo permanente termina en `failed`. Las actualizaciones posteriores de la reserva no disparan otro envío y ningún fallo del email modifica o cancela la reserva, que puede seguir `pending` por asignación.

**Ownership and visibility:** solo Firebase Functions mediante Admin SDK crea o actualiza estos documentos. Admin puede leerlos para operación; guest y client no tienen lectura ni escritura. No requiere índice porque se consulta por ID determinístico.

**Rollback:** el cambio es aditivo. Para revertirlo, deshabilitar o retirar `onReservaConfirmationCreated`, conservar `confirmaciones` para auditoría y no borrar documentos ni ejecutar una migración destructiva.

---

### `empleados/{empId}`

Datos de staff y disponibilidad para la asignación de reservas.

| Campo | Tipo | Generated by | Notas |
|---|---|---|---|
| `name` | string | admin | nombre operativo |
| `role` | 'groomer' \| 'bañador' \| 'cuidador' | admin | rol del empleado |
| `photoUrl` | string \| null | admin | recurso opcional |
| `active` | boolean | admin | baja lógica cuando es `false` |
| `services` | string[] | admin | IDs de `servicios` que puede atender |
| `weeklyShifts` | map | admin | una clave por día: `morning`, `afternoon`, `full` o `null` |

`weeklyShifts` contiene las siete claves `monday`, `tuesday`, `wednesday`, `thursday`, `friday`, `saturday` y `sunday`. `morning` cubre 08:00-14:00, `afternoon` 14:00-20:00 y `full` 08:00-20:00.

**Ownership:** solo administradores pueden leer, crear, actualizar o eliminar documentos de `empleados`. La asignación de `reservas.empleadoId` pertenece a Firebase Functions/Admin SDK; no es un campo controlado por el cliente.

**Reglas:** admin-only total.

---

### Backfill opcional de `reservas.empleadoId`

El comando es idempotente y funciona en modo dry-run por defecto. Lee `reservas` por lotes y, únicamente con `--apply`, agrega `empleadoId: null` a documentos que no tienen el campo. No asigna empleados ni modifica otros campos. En ambos modos emite un manifiesto JSON con los IDs que serían o fueron afectados; en modo `--apply` solo incluye IDs confirmados dentro de la transacción.

```bash
# Emulador: inspección sin escrituras (modo por defecto)
node tools/backfill-empleado-id.mjs --emulator --manifest /tmp/backfill-empleado-id-dry-run.json

# Emulador: aplicar la normalización
node tools/backfill-empleado-id.mjs --emulator --apply --manifest /tmp/backfill-empleado-id-apply.json

# Cuenta de servicio: siempre indicar explícitamente el archivo
node tools/backfill-empleado-id.mjs --service-account /ruta/serviceAccount.json [--apply] [--manifest /ruta/backfill-manifest.json]
```

El manifiesto contiene `reservationIds`, `mode`, `count` y el campo normalizado. Consérvalo junto con la evidencia de la ejecución. Este backfill es independiente de ADR-008: la callable no lo requiere y esta decisión no inventa ni autoriza una migración de reservas existentes. Esta tarea no ejecuta el backfill contra producción. Antes de cualquier ejecución productiva futura debe existir un backup verificado y autorización explícita; la reversión se limita a restaurar desde ese backup o a seguir un procedimiento aprobado que use exclusivamente un manifiesto verificado. No se recomienda borrar manualmente `empleadoId` como rollback ni ejecutar una operación destructiva.

---

### `config/{docId}`

Configuración global del negocio. Prevista pero sin uso en Fase 2.

**Reglas:** admin-only total.

---

### `mensajes/{msgId}` (propuesto en T2.5)

Mensajes del form de contacto.

| Campo | Tipo | Notas |
|---|---|---|
| `name` | string | |
| `email` | string | |
| `message` | string | |
| `createdAt` | Timestamp | `serverTimestamp()` |
| `read` | boolean | marca de leído para panel admin (default false) |

**Reglas (a agregar en T2.5):** `allow create: if true` (cualquiera puede escribir sin login); read/write admin-only.

---

## Colecciones que NO existen

- `galeria` — existe en `firestore.rules` pero no se usa: T2.7 decidió imágenes estáticas en `public/galeria/` (vía ADR-003). Al limpiar Storage, evaluar si quitar la regla de `galeria` también.
